# CLAUDE.md

このリポジトリで作業するエージェント / 開発者が必ず守る開発ルール。

## 開発ルール

### 1. TDD（テスト駆動開発）を徹底する

- 実装コードより先にテストを書く（Red → Green → Refactor）。
- テストのないコードをコミットしない。
- **Red とは「新しいテストが、個別に、狙った理由で落ちること」**。落ちた件数の合計では
  判断しない。5 件追加して 3 件しか落ちないなら、**通った 2 件のほうを調べる**。
  条件を再現できていないテストは、修正前のコードでも緑になる。
- **バグ修正では、修正を一時的に無効化して新しいテストが赤くなることを確認する。**
  戻して緑に戻ることまで見る。これをやらないと、「テストを追加した」という事実だけが
  残り、実際には何も守られていない状態になる。
- **コードを消しても通るテストは、観測点が間違っている。** テスト対象の分岐を削除して
  なお緑なら、直すべきはコードではなくアサーションの取り方。
- カバレッジではここは守れない。上のいずれの失敗も、行は実行されるので数値には現れない。
- **カバレッジは 90% 以上を必須**とする（statements / branches / functions / lines）。
  閾値は `vitest.config.ts` に設定済みで、下回るとテストが失敗する。
- カバレッジを満たすためだけの無意味なテストは書かない。振る舞いを検証する。

### 2. 推測でコードを書かない（必ずコードリーディングをする）

- 既存の実装・型・テストを読まずに API や挙動を推測して実装しない。
- 外部ライブラリの挙動が不明な場合は、型定義・ドキュメント・実際の出力を確認してから使う。
- 「たぶんこう動くはず」で PR を出さない。必ず実行して確認する。

### 3. TiDD（チケット駆動開発）を徹底する

- **ISSUE のない実装は不可**。作業前に必ず ISSUE を作成する。
- 1 つの ISSUE に対して 1 つのブランチ / 1 つの PR を対応させる。
- 作業中に判明した仕様変更・追加作業は ISSUE を更新するか、新しい ISSUE を切る。
- 例外: 初期構築（イニシャル）に限り、粒度の大きいエピックチケットで構わない。

### 4. ISSUE のライフサイクル管理を徹底する

- 着手前: ISSUE を**作成**する（背景 / スコープ / スコープ外 / 完了条件を書く）。
- 作業中: 判明した事実・方針変更を ISSUE に**更新**する。
- 完了時: PR 本文に `Closes #<issue番号>` を書き、**PR のマージによってクローズ**する。
  手動での ISSUE クローズは原則行わない（実装されていないものを閉じないため）。

### 5. コミットメッセージは英語で書く

- 件名・本文とも英語。命令法・現在形の件名(`Add …` / `Fix …`)を使う。
- **なぜそう変更したか**を本文に書く。何を変更したかは diff を読めば分かる。
- 既に `main` にマージ済みのコミットは書き換えない。履歴の書き換えは
  force-push を伴い、既存のクローンを壊し、ISSUE から参照済みのコミットリンクを
  無効にする。メッセージの言語を揃えるためだけに払うコストではない。
- ISSUE / PR 本文の言語はこのルールの対象外(現状は日英混在)。

### 6. CHANGELOG を同じ PR で更新する

- 利用者から観測できる変更(API 追加 / 破壊的変更 / 挙動変更 / バグ修正)を含む PR は、
  `CHANGELOG.md` の `Unreleased` に項目を追加する。
- リリース時にまとめて書き起こさない。**なぜその変更が必要だったか**が新鮮なうちに書く。
  後から diff を見て再構成すると、変更内容の再説明にしかならない。
- リリースを切るときに `Unreleased` をバージョン見出しに変える。
- 内部リファクタリング・テストのみ・ドキュメントのみの変更は対象外。

## コマンド

```bash
npm install          # 依存インストール
npm test             # テスト実行（カバレッジ 90% 閾値つき）
npm run test:watch   # ウォッチ実行
npm run typecheck    # 型チェック
npm run build        # dist/ へビルド
```

## アーキテクチャ方針

- ブラウザ API（`AudioContext` / `navigator.gpu` / `indexedDB`）には**直接依存しない**。
  すべて interface 経由で注入し、テストから差し替え可能にする（`src/platform.ts`）。
- 推論エンジンは `SynthesisEngine` interface の背後に隠す。
  実モデル（ONNX Runtime Web）は後続 ISSUE で差し込む。
- 副作用のないロジック（テキスト処理 / 音声変換）は純粋関数として切り出し、単体テストする。
