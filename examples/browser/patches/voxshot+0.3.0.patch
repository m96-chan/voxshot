diff --git a/node_modules/voxshot/dist/engine/chatterbox/chatterbox-engine.d.ts b/node_modules/voxshot/dist/engine/chatterbox/chatterbox-engine.d.ts
index 0788743..c7490b6 100644
--- a/node_modules/voxshot/dist/engine/chatterbox/chatterbox-engine.d.ts
+++ b/node_modules/voxshot/dist/engine/chatterbox/chatterbox-engine.d.ts
@@ -82,6 +82,17 @@ export interface ChatterboxEngineOptions {
      * @defaultValue 0.5
      */
     exaggeration?: number;
+    /**
+     * Classifier-free guidance scale. Values above 1 enable CFG; 1.5 matches
+     * the reference implementation's `cfg_weight=0.5`. Required for the
+     * Chatterbox multilingual checkpoint, which is unintelligible without it.
+     *
+     * Needs a build of `@huggingface/transformers` that supports it — see
+     * `examples/browser/patches`.
+     *
+     * @defaultValue 1 (disabled)
+     */
+    guidanceScale?: number;
     /**
      * Called with model download progress forwarded from Transformers.js, and
      * with the engine's own {@link ChatterboxLifecycleEvent} milestones.
diff --git a/node_modules/voxshot/dist/engine/chatterbox/chatterbox-engine.js b/node_modules/voxshot/dist/engine/chatterbox/chatterbox-engine.js
index 9468996..279cab6 100644
--- a/node_modules/voxshot/dist/engine/chatterbox/chatterbox-engine.js
+++ b/node_modules/voxshot/dist/engine/chatterbox/chatterbox-engine.js
@@ -114,6 +114,7 @@ export class ChatterboxEngine {
     #dtypeOverrides;
     #maxNewTokens;
     #exaggeration;
+    #guidanceScale;
     #onProgress;
     #loadModule;
     #supportsFp16;
@@ -134,6 +135,10 @@ export class ChatterboxEngine {
         this.#dtypeOverrides = options.dtype;
         this.#maxNewTokens = validateMaxNewTokens(options.maxNewTokens);
         this.#exaggeration = options.exaggeration ?? DEFAULT_EXAGGERATION;
+        // Classifier-free guidance. Only meaningful above 1; the Chatterbox
+        // multilingual checkpoint is unintelligible without it, and 1.5 is
+        // parity with the reference implementation's cfg_weight=0.5.
+        this.#guidanceScale = options.guidanceScale ?? 1;
         this.#onProgress = options.onProgress;
         this.#loadModule = options.loadModule ?? defaultModuleLoader;
         this.#supportsFp16 = options.supportsFp16 ?? webGpuSupportsFp16;
@@ -329,6 +334,9 @@ export class ChatterboxEngine {
                 exaggeration: expressiveness,
                 max_new_tokens: budget,
                 ...(criteria.length > 0 ? { stopping_criteria: criteria } : {}),
+                // Omitted at 1 so a build without CFG support behaves exactly
+                // as before rather than tripping over an unknown option.
+                ...(this.#guidanceScale > 1 ? { guidance_scale: this.#guidanceScale } : {}),
             });
         }
         finally {
